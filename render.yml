services:
  - type: web
    name: fastapi-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: MONGODB_URI
        value: ${mongodb+srv://musab:lkLcVq0MOOxpLxxo@cluster0.9r682mt.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0}
      - key: ELASTICSEARCH_URL
        value: ${https://my-elasticsearch-project-a38744.es.us-central1.gcp.elastic.cloud:443}
